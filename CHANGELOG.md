# Changelog

## Unreleased

Added GitHub Actions CI to build the XLA PJRT CPU plugin and run PJRT loader smoke tests on push/PR, plus a JAX plugin smoke test harness.
I interrupted the local Bazel build during setup and will finish validating the full build and JAX smoke run on my machine later.
